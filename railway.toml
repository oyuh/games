[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @games/api build"

[deploy]
startCommand = "node apps/api/dist/apps/api/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
